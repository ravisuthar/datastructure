package java8;

import java.util.function.Function;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

public class ToMap {
    public static void main(String[] args) {

        //var collect = IntStream.range(0, 10).collect(Collectors.toMap(i -> i, v));


    }
}
